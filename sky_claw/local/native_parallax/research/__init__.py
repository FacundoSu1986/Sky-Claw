"""Spike numérico NP-M0: height sintético → normal → reconstrucción periódica → métricas.

Research-only. Sin DDS, sin Skyrim, sin MO2, sin IA, sin ParallaxR (clean-room:
solo matemática pública + NumPy). Ver ``run_np_m0.py`` para ejecutar el experimento.
"""
